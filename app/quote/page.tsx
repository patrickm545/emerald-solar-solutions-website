import { QuotePage } from "@/components/marketing-pages";
import { quotePageMetadata } from "@/lib/seo";

export const metadata = quotePageMetadata;

export default function Page() {
  return <QuotePage />;
}
